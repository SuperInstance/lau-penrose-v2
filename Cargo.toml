[package]
name = "lau-penrose-v2"
version = "0.1.0"
edition = "2021"
description = "Penrose correlation engine v2 — detecting and predicting correlations between PLATO rooms"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
